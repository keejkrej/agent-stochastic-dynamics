# PRL stub -- do not submit yet

PRL is not the first venue. A Physical Review Letter needs one crisp result, not a framework.

Candidate letter (only if the figure is real):

Title (draft): Numerical noise is a boundary layer; cascade is a coupling exponent

Result: (i) token flips from logit perturbation occur only on the near-tie set Delta ell <= 2 ||eps||_inf; (ii) a one-bit memory split expands in Hamming on prefixes (TV cannot expand). One figure: P(flip) vs eta with the 2-eta marker, and Hamming(n) for a coupled pair.

Until those two panels come from a production stack (vLLM / llama.cpp / OpenRouter) with replay, this folder stays a stub. ICLR/ICML first.
