{
  "name": "agent-stochastic-dynamics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed-noise stochastic dynamics for LLM agents; vdom as control on the kernel.",
  "scripts": {
    "test": "tsx --test src/tests/lemmas.test.ts",
    "typecheck": "tsc --noEmit",
    "experiments": "tsx src/experiments.ts",
    "live": "tsx src/live.ts"
  },
  "devDependencies": {
    "@types/node": "^22.17.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
